name: projeto_final_faculdade
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  routefly: ^1.0.5
  flex_color_scheme: ^7.3.1
  sqflite: ^2.3.0
  cupertino_icons: ^1.0.2
  flutter_animate: ^4.2.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
